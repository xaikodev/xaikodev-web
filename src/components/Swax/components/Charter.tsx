import { Stack } from "@chakra-ui/react";
import { FC } from "react";

interface CharterProps {}
export const Charter: FC<CharterProps> = (props) => {
  const {} = props;
  return <Stack></Stack>;
};
